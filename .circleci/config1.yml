version: 2.1

parameters:
  manual:
    type: boolean
    default: false
  file:
    type: enum
    enum: [config1, config2]
    default: config1

jobs:
  say-hello:
    docker:
      - image: cimg/base:stable
    steps:
      - checkout
      - run:
          name: "Say hello"
          command: "echo Hello, World2!"

workflows:
  say-hello-workflow1:
    jobs:
      - say-hello
